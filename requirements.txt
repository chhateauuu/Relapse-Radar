# Relapse Radar — Python dependencies (brain, simulator, api, llm)
# Web (P4/P5) deps live in web/package.json.

# Brain (P1 + P2)
pandas
numpy<2          # shap/lightgbm/scipy in this env are built against numpy 1.x; numpy 2.x breaks them
scikit-learn
lightgbm
ruptures
shap<0.46        # 0.46+ requires numpy>=2; pin keeps it compatible with numpy<2
matplotlib

# API + escalation (P3)
fastapi
uvicorn[standard]
twilio
python-dotenv

# LLM (P3) — pick one; on-device Ollama for the privacy story in prod
openai
